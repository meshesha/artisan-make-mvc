$EXTEND_LAYOUTS$

$YIELD_SECTION$
<div class = "container">
    <div class="card mt-5">
        <h1 class="card-header d-flex justify-content-between align-items-center">
            $HEAD_TITLE$
            <div>
                <a href="{{ url()->previous() }}" class="btn btn-info">Go Back</a>
                <a href="{{route('$TABLE_NAME$.create')}}"  class="btn btn-primary">Create $MODAL_NAME_LOWER$</a>
            </div>
        </h1>
        <div class="card-body">
            <table class = "table table-bordered table-hover">
                <thead>
                    <tr class="table-primary">
                        $TABLETH$
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                @if(count($$TABLE_NAME$) > 0)
                    @foreach($$TABLE_NAME$ as $$MODAL_NAME_LOWER$)
                        <tr>
                            $TABLEBODYTD$
                            <td>
                                <div class="btn-group" role="group" aria-label="Action buttons">
                                    <a href="{{route('$TABLE_NAME$.show', $$MODAL_NAME_LOWER$->$PRIMARY_KEY_NAME$)}}" class="btn btn-info btn-sm">Show</a>
                                    <a href="{{route('$TABLE_NAME$.edit', $$MODAL_NAME_LOWER$->$PRIMARY_KEY_NAME$)}}" class="btn btn-primary btn-sm">Edit</a>
                                    <form method="POST" action="{{ route('$TABLE_NAME$.destroy',  $$MODAL_NAME_LOWER$->$PRIMARY_KEY_NAME$) }}">
                                        @csrf
                                        @method('DELETE')
                                        <button type="submit" 
                                            class="btn btn-danger btn-sm"
                                            onclick="return confirm('Are you sure you want to delete this $MODAL_NAME_LOWER$?')">
                                            Delete
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    @endforeach
                @endif
                </tbody>
            </table>
        </div>
        <div class="card-footer text-muted">
            <!-- footer text here -->
        </div>
    </div>
</div>
$YIELD_SECTION_END$